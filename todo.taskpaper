grandma-watcher:

	Prep:
		- Define domain types in models.py (AssessmentResult, Alert, SensorSnapshot) @done
		- Write full config.yaml schema with defaults and comments @done
		- Set up pyproject.toml with ruff, black, and pytest configured @done
		- Create Makefile with single pre-merge check command @done
		- Create tests/ scaffold with conftest.py and a fixture JPEG for mocking the camera @done
		- Document local dev strategy in context/dev-environment.md @done

	Milestone 1 - Core Monitor Loop:
		- Implement models.py dataclasses @done
		- Implement prompt_builder.py (builds VLM prompt from config + sensor snapshot) @na
		- Implement VLM response parser (raw JSON → AssessmentResult; malformed → safe/low fail-safe) @na
		- Implement OpenRouterProvider satisfying VLMProvider protocol @na
		- Implement alert decision matrix in alert.py (PRD §6.3 table: safe/confidence → action) @na
		- Implement alert sliding window + cooldown logic in alert.py @na
		- Implement patient_location state machine in alert.py (auto-silence / auto-resume) @na
		- Implement PushoverChannel satisfying AlertChannel protocol @na
		- Implement dataset.py (append DatasetEntry to log.jsonl, save image) @na
		- Wire monitor.py core loop (fetch frame → assess → decide → alert → log) @na
		- Integration test: full cycle with mocked camera, mocked OpenRouter, mocked Pushover @na
		- Smoke test on Pi: verify real camera frame flows through and alert fires correctly @na
